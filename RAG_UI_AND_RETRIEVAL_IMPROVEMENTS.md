# تحسينات RAG والواجهة

تمت إضافة تحسينات عملية على طبقتين: الاسترجاع والتجربة البصرية.

## 1) تحسين الاسترجاع

### الملفات الجديدة/المعدلة

- `src/lib/query-expansion.ts`
- `src/lib/rag.ts`
- `src/lib/gemini.ts`
- `src/app/api/interpret/route.ts`
- `src/app/page.tsx`
- `src/app/globals.css`

### ماذا أضيف؟

- طبقة Query Expansion لفهم اللهجة والرموز الحديثة.
- تحويل الرموز الحديثة إلى رموز قديمة قريبة:
  - الرصاص / إطلاق النار → سهم، رمي، سلاح، جرح، طعن، قتل.
  - الظهر → خلف، دبر، وراء، أذى غير مباشر.
  - النجدة → استغاثة، استنصار، طلب عون.
  - أمي/الوالدة → أم، والدة، ابنها.
  - طلقتين → سهمين، ضربتين، جرحين، مرتين.
- معالجة التباس كلمة `طالقين علي` حتى لا تُفهم كطلاق زوجي عند وجود سياق الرصاص.
- تقليل ترتيب النتائج الخادعة مثل:
  - الرصاص كمعدن.
  - الطلاق الزوجي.
  - سياقات الصنم/العبادة إذا كان سؤال المستخدم عن رصاصة.
- إضافة عوامل ترتيب جديدة لكل مقطع:
  - `semanticScore`
  - `coverageScore`
  - `falseFriendPenalty`
  - `matchedFocusTerms`
  - `rerankReason`

## 2) تحسين Prompt Gemini

تم تعديل التعليمات بحيث:

- يفرق Gemini بين النص المباشر والقياس الرمزي.
- لا يرفع قوة الاستشهاد إذا كانت المقاطع غير مرتبطة.
- لا يفسر `طلق/طلقتين` كطلاق زوجي في سياق الرصاص.
- يذكر جودة الاستدلال وحدود الجواب.
- يرجع `symbolAnalysis` و `evidenceQuality` داخل JSON.

## 3) تحسين الواجهة

- واجهة حديثة Glass UI.
- أنميشن دخول للكروت.
- عرض مراحل التحليل تدريجيًا.
- كتابة جواب النموذج بتأثير Typewriter.
- ديباق بحث واضح يتضمن:
  - النص المطبع.
  - الاستعلام الموسع.
  - قواعد التوسيع التي اشتغلت.
  - تحذيرات الالتباس.
  - مصطلحات التركيز.
  - عوامل ترتيب كل مقطع.
- حفظ آخر نتيجة في `localStorage` حتى لا تختفي بعد refresh.

## التشغيل

```bash
pnpm install
pnpm rag:build
pnpm dev
```

أو باستخدام npm:

```bash
pnpm install
pnpm run rag:build
pnpm run dev
```

## التحقق

تم اختبار البناء بنجاح عبر:

```bash
pnpm run build
```
